services:
  web:
    build:
      context: .
      target: runner
    container_name: lux-cloud-web
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_IAM_HOST: https://lux.id
      NEXT_PUBLIC_API_HOST: https://api.lux.cloud
    restart: unless-stopped
